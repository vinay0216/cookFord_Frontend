/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['i.pinimg.com', 'static.cinemagia.ro', 'mir-s3-cdn-cf.behance.net'], // Add other domains if needed
    },
  };
  
  export default nextConfig;
  